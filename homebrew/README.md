# Homebrew

This folder contains the bundles to use based on the workstation of choice

## Getting Started

Install Brew

```zsh
./setup-dev.sh
```

Install bundle

```zsh
cd ./personal
brew bundle install
brew bundle install --cleanup
```